{
  "version": 2,
  "installCommand": "cd ../backend && npm install && cd ../frontend && npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://c-personal-1-uukg.onrender.com/api/$1" }
  ]
}
